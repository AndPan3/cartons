[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "cartons"
version = "1.0.0"
description = "Simple OSRM routing and Folium map drawing toolkit"
requires-python = ">=3.7"

dependencies = [
  "routingpy",
  "folium"
]
